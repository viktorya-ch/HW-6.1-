 CREATE TABLE dynamic_rule(
 id UUID PRIMARY KEY,
 product_id UUID,
 product_name VARCHAR(255)
 product_text TEXT
 );



 CREATE TABLE rule_condition(
 id UUID PRIMARY KEY,
 rule_id UUID REFERENCES
 dynamic_rule(id) ON DELETE CASCADE,
 query_type VARCHAR(50),
 arguments JSONB,
 negate BOOLEAN
 );